import React from 'react'
import Login from '../components/Layout/Login'

export const metadata = {
    title: 'Battle of the Bots | Register',
}

const page = () => {
    return (
        <div>
            <Login />
        </div>
    )
}

export default page
